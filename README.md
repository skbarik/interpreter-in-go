# Write an interpreter in Go
Going through the book https://interpreterbook.com/ 

Sample Monkey language
```
let x = 1;
let y = 2;
let add = fn(x,y) {
    return x + y;
};
let result = add(x,y);
```
